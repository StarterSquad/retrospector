// External deps
var _ = require('underscore');

// Internal deps
var middleware = require('./middleware');


module.exports = function (server) {
//  server.get('/api/attachments/:id/download', [middleware.isLoggedIn], attachments.download);
//  server.post('/api/attachments', [middleware.isLoggedIn], attachments.upload);
//  server.del('/api/attachments/:id', [middleware.isLoggedIn], attachments.remove);
};
